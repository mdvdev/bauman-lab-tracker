export type User = {
    id: string,
    email: string,
    firstName: string,
    lastName: string,
    group: string,
    patronymic: string,
    telegramUsername: string,
    roles: string[],
    createdAt: string,
    photoUri: string
};
